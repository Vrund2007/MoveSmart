// components/recommendations/LocalityCard.jsx — Card displaying a recommended locality with 4-district scores (PRD §5, Architecture.md §5)
// Shows: locality name, Residential/Business/Lifestyle/Transit scores, recommended badge (primary #00ADB5)
// TODO: accept locality, scores, rank, explanation props
// TODO: highlight top-ranked locality with primary badge per Design.md §2.1
function LocalityCard() {
  return <div>LocalityCard — TODO</div>;
}
export default LocalityCard;
