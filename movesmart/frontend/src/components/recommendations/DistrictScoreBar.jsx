// components/recommendations/DistrictScoreBar.jsx — Visual bar showing a single district dimension score (PRD §5, Design.md §2)
// One bar per: Residential, Business, Lifestyle, Transit
// TODO: accept dimension (string), score (0-100), weight props
// TODO: render filled bar in primary color proportional to score
function DistrictScoreBar() {
  return <div>DistrictScoreBar — TODO</div>;
}
export default DistrictScoreBar;
