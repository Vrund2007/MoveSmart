// components/assistant/ChatWidget.jsx — Conversational AI assistant chat UI (Architecture.md §4.1 Layer 5, PRD §7.1)
// Calls POST /api/assistant/chat; Gemini API called server-side only (Rules.md §5)
// TODO: implement message input, send button, message history display
// TODO: handle Gemini rate-limit errors with graceful user-facing message (Rules.md §4)
// TODO: show loading state while waiting for response
function ChatWidget() {
  return <div>ChatWidget — TODO</div>;
}
export default ChatWidget;
