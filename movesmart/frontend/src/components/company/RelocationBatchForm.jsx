// components/company/RelocationBatchForm.jsx — Form to start a new relocation batch (new v2.0, PRD §7.4, Architecture.md §4.4)
// Collects: office location, headcount, budget per employee, employee list
// Submits to POST /api/company/relocation-batches
// TODO: implement form fields for batch creation
// TODO: employees[] embedded in batch (database.md §3.7)
function RelocationBatchForm() {
  return <div>RelocationBatchForm — TODO</div>;
}
export default RelocationBatchForm;
