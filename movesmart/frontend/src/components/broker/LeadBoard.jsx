// components/broker/LeadBoard.jsx — Broker's lead pipeline board (new v2.0, PRD §7.3, Architecture.md §4.3)
// Displays enquiries across broker's managed listings grouped by lead_status: new|contacted|converted|lost
// TODO: fetch leads from GET /api/leads?broker=me
// TODO: implement status update via PATCH /api/leads/:id
// TODO: leads are only visible to the broker — never to other roles (FR-7)
function LeadBoard() {
  return <div>LeadBoard — TODO</div>;
}
export default LeadBoard;
