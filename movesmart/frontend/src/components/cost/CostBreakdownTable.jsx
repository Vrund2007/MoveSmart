// components/cost/CostBreakdownTable.jsx — Itemized cost-of-living breakdown table (Architecture.md §4.1 Layer 4, Design.md §3.4)
// Applies tabular-nums (font-variant-numeric: tabular-nums) for aligned numeric columns (Design.md §3.4)
// Estimates must be clearly labeled as estimates — not measured data (Rules.md §3)
// TODO: accept locality, costItems (rent, food, transport, utilities, etc.) props
// TODO: apply .tabular-nums CSS class to all numeric cells
// TODO: include an "estimate disclaimer" label in Micro/Small typography per Design.md §3.5
function CostBreakdownTable() {
  return <div>CostBreakdownTable — TODO</div>;
}
export default CostBreakdownTable;
