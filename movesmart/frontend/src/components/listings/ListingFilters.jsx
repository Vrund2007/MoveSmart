// components/listings/ListingFilters.jsx — Filter bar for listings browse (Architecture.md §8: GET /api/listings filterable by locality, bhk, budget, deal_type)
// TODO: implement filter controls: locality (text/select), BHK, price range, deal_type (rent|buy)
// TODO: call onChange callback with filter state for parent to pass to API
function ListingFilters() {
  return <div>ListingFilters — TODO</div>;
}
export default ListingFilters;
