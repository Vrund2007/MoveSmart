// components/listings/ListingCard.jsx — Card for a single listing (Architecture.md §5, PRD §7.1)
// Displays: title, price (tabular-nums per Design.md §3.4), locality, BHK, deal_type, TrustBadge, StatusBadge
// Only shows approved listings to Find Accommodation users — status filter is enforced server-side (FR-3)
// TODO: accept listing object prop
// TODO: include TrustBadge (Isolation Forest output) and StatusBadge (pending/approved/rejected)
function ListingCard() {
  return <div>ListingCard — TODO</div>;
}
export default ListingCard;
