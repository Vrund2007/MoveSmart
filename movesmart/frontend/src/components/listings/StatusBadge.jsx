// components/listings/StatusBadge.jsx — Listing approval status badge (new v2.0, Architecture.md §5)
// Maps: pending_review → warning (#F59E0B), approved → success (#22C55E), rejected → error (#EF4444) per Design.md §2
// Visible to Owner/Broker on their own listings dashboard; not shown to Find Accommodation users (FR-7)
// TODO: accept status ('pending_review'|'approved'|'rejected') prop
// TODO: map status to Design.md color tokens
function StatusBadge() {
  return <span>StatusBadge — TODO</span>;
}
export default StatusBadge;
