// components/listings/TrustBadge.jsx — Visual badge for Isolation Forest suspicious-listing flag (Architecture.md §7, Rules.md §7)
// Uses Warning color (#F59E0B) per Design.md §2.1 — "looks unusual", never states "is fake" (Rules.md §7, §9)
// TODO: accept isSuspicious (boolean), checkedAt (date) props
// TODO: only render if isSuspicious is true; use hedged copy: "looks unusual compared to similar listings"
function TrustBadge() {
  return <div>TrustBadge — TODO</div>;
}
export default TrustBadge;
