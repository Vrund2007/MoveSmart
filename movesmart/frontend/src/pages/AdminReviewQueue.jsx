// pages/AdminReviewQueue.jsx — Admin listing review queue page (new v2.0, PRD §7.5, Architecture.md §4.5)
// Only accessible to admin-role accounts — enforced server-side by IsAdmin permission class (FR-4)
// Admin accounts are provisioned manually, never via public signup (FR-2)
// TODO: render ReviewQueueTable component listing all pending_review listings
// TODO: render ApproveRejectPanel on row selection
function AdminReviewQueue() {
  return <div>AdminReviewQueue — TODO</div>;
}
export default AdminReviewQueue;
