// pages/Dashboard.jsx — Find Accommodation main dashboard: Layer 1–5 results (PRD §7.1, Architecture.md §4.1)
// Layers: LocalityCard recommendations → ListingCard grid → CommutePanel → CostBreakdownTable → ChatWidget
// All listing data comes from approved listings only (FR-3, enforced server-side)
// TODO: implement 5-layer layout (recommendations, listings, commute, cost, assistant)
// TODO: all API calls must handle loading and error states per Rules.md §4
function Dashboard() {
  return <div>Dashboard (Find Accommodation) — TODO</div>;
}
export default Dashboard;
