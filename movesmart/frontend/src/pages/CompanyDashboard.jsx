// pages/CompanyDashboard.jsx — Company/HR relocation dashboard (new v2.0, PRD §7.4, Architecture.md §4.4)
// HR sees: relocation batches, RelocationBatchForm, BulkSearchResults, AllocationGrid, BudgetTracker
// Uses same area-scoring engine as Find Accommodation for bulk search (FR-8)
// TODO: integrate RelocationBatchForm, BulkSearchResults, AllocationGrid, BudgetTracker components
// TODO: fetch batches from GET /api/company/relocation-batches (own company's batches only — FR-7)
function CompanyDashboard() {
  return <div>CompanyDashboard — TODO</div>;
}
export default CompanyDashboard;
